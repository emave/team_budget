import { createLightTheme, createDarkTheme } from 'baseui';
import { FONT_FAMILY } from './fonts';

export const lightTheme = createLightTheme({ primaryFontFamily: FONT_FAMILY });
export const darkTheme = createDarkTheme({ primaryFontFamily: FONT_FAMILY });
